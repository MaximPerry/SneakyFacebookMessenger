{
  "name": "fbcamouflage",
  "productName": "FB Camouflage",
  "description": "A subtile way to spend time on Facebook.",
  "version": "0.0.2",
  "main": "main.js",
  "devDependencies": {
    "electron": "^1.8.8",
    "electron-packager": "^8.7.2",
    "electron-winstaller": "^2.6.4",
    "extract-zip": "^1.0.3"
  },
  "scripts": {
    "compile": "electron-packager . FacebookCamouflage --icon=img/icon.ico --out=release-builds --platform=win32 --arch=x64 --asar --overwrite",
    "create-installer-win": "node installers/windows/createinstaller.js"
  },
  "dependencies": {
    "path": "^0.12.7"
  }
}
